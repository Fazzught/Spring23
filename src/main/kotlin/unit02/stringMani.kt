fun main(args: Array<String>) {
    print("Enter the name of you're favorite city: ")
    var city = readLine()!!
    println(city.length)
    println(city.toUpperCase())
    println(city.toLowerCase())
    println(city[0])
}